export type User = {
  id: number;
  name: string;
  profilePicture: string;
  headerPictures: string[];
  api_key: string;
};

export type Game = {
  id: number;
  name: string;
  image: string;
  website: string;
};

export type Series = {
  id: number;
  name: string;
  image: string;
  website: string;
};

export type Music = {
  id: number;
  name: string;
  image: string;
  website: string;
};
